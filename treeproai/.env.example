# TreeProAI Environment Variables
# ---------------------------------
# Copy this file to .env and fill in the values for your local development.
# Do not commit .env to version control.

# --- PostgreSQL Database (Phase 1) ---
# Connection string for your PostgreSQL database.
DATABASE_URL="postgresql://user:password@localhost:5432/treeproai"

# --- PII Encryption (Phase 1) ---
# A 32-byte (256-bit) key, base64-encoded, for encrypting PII data.
# Generate one with: openssl rand -base64 32
PII_ENCRYPTION_KEY="your-base64-encoded-256-bit-encryption-key"

# --- NestJS API (Phase 2) ---
# The port the API will run on.
API_PORT=4000

# --- Redis / BullMQ (Phase 4) ---
# Connection string for your Redis instance.
REDIS_URL="redis://localhost:6379"

# --- Clerk Authentication (Phase 2 & 6) ---
# Get these from your Clerk.dev dashboard.
# Used by the Next.js frontend.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
# Used by the NestJS backend.
CLERK_SECRET_KEY="sk_test_..."

# --- Frontend URLs (Phase 6) ---
# These are used by Clerk for redirects.
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/

# --- S3-Compatible Storage (e.g., MinIO, AWS S3) (Phase 2) ---
# For local development with MinIO, this would be http://localhost:9000
S3_ENDPOINT="your-s3-endpoint"
S3_BUCKET="treeproai"
S3_ACCESS_KEY_ID="your-access-key"
S3_SECRET_ACCESS_KEY="your-secret-key"
S3_REGION="us-east-1" # Or your preferred region

# --- AI Services (Phase 3) ---
# URLs for the FastAPI services.
AI_VISION_URL="http://localhost:8001"
AI_PRICING_URL="http://localhost:8002"

# --- Stripe Payments (Phase 8) ---
# Get these from your Stripe dashboard.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# --- Frontend API Communication ---
# The base URL for the NestJS API, used by the Next.js frontend.
NEXT_PUBLIC_API_BASE_URL="http://localhost:4000"