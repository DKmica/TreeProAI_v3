# -----------------------------------------------------------------------------
# This file contains example environment variables for the TreeProAI monorepo.
# Copy this file to .env in the root of the 'treeproai' directory and fill in
# the values for your local development environment.
# -----------------------------------------------------------------------------

# -- PostgreSQL Database --
# Used by Drizzle ORM and the API service.
DATABASE_URL="postgresql://postgres:password@localhost:5432/treeproai"

# -- Redis (for BullMQ Queues) --
# Used by the API and Worker services for background jobs.
REDIS_URL="redis://localhost:6379"

# -- S3-Compatible Storage (MinIO) --
# Used for file uploads (e.g., quote request images).
S3_ENDPOINT="http://localhost:9000"
S3_BUCKET="treeproai"
S3_ACCESS_KEY="minioadmin"
S3_SECRET_KEY="minioadmin"

# -- Authentication (Clerk) --
# Used for user authentication and management.
# Get these from your Clerk dashboard.
CLERK_SECRET_KEY="sk_test_..."
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
NEXT_PUBLIC_CLERK_SIGN_IN_URL="/sign-in"
NEXT_PUBLIC_CLERK_SIGN_UP_URL="/sign-up"
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL="/dashboard"
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL="/dashboard"

# -- Payments (Stripe) --
# Used for processing invoice payments.
# Get these from your Stripe dashboard.
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."

# -- Google Maps API --
# Used for maps, address lookups, and routing suggestions.
# Get this from the Google Cloud Console.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY="AIza..."

# -- Application URLs & Ports --
# Configure the ports and URLs for local development.
API_PORT=4000
NEXT_PUBLIC_API_URL="http://localhost:4000/v1"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# -- PII Encryption --
# A 32-byte hex-encoded key for encrypting personally identifiable information.
# Generate one with: openssl rand -hex 32
PII_ENCRYPTION_KEY=""